import { ElementFilterPipe } from './element-filter.pipe';

describe('ElementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
